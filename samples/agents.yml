agents:
  # =============================================================================
  # A2A Protocol Agents
  # =============================================================================
  check_prime_agent:
    version: 1.0.0
    url: http://host.docker.internal:8001/a2a/check_prime_agent
    bearer: ""
    protocol: a2a
    protocolVersion: 0.3.0
    metadata:
      agentCard:
        url: "http://host.docker.internal:8001/a2a/check_prime_agent/.well-known/agent-card.json"
        name: check_prime_agent
        version: "1.0.0"
        description: An agent specialized in checking whether numbers are prime. It can efficiently determine the primality of individual numbers or lists of numbers.
        capabilities: {}
        defaultInputModes:
          - text/plain
        defaultOutputModes:
          - application/json
        skills:
          - id: prime_checking
            name: Prime Number Checking
            description: Check if numbers in a list are prime using efficient mathematical algorithms
            tags:
              - math
              - prime
              - numbers

  # =============================================================================
  # MCP Protocol Agents (Model Context Protocol)
  # =============================================================================

  # Example: Remote MCP server via HTTP (streamable-http transport)
  # exa_search:
  #   protocol: mcp
  #   protocolVersion: 2024-11-05
  #   transport:
  #     type: streamable-http
  #     url: https://mcp.exa.ai/mcp
  #     headers:
  #       Authorization: Bearer ${EXA_API_KEY}
  #   metadata:
  #     description: Exa AI search MCP server

  # Example: Remote MCP server via SSE (legacy transport)
  # remote_mcp_sse:
  #   protocol: mcp
  #   protocolVersion: 2024-11-05
  #   transport:
  #     type: sse
  #     url: https://example.com/mcp/sse
  #     headers:
  #       X-API-Key: ${API_KEY}

  # Example: Local MCP server via STDIO (filesystem access)
  # local_filesystem:
  #   protocol: mcp
  #   protocolVersion: 2024-11-05
  #   transport:
  #     type: stdio
  #     command: npx
  #     args:
  #       - "-y"
  #       - "@modelcontextprotocol/server-filesystem"
  #       - "/home/user/documents"
  #   metadata:
  #     description: Local filesystem MCP server

  # Example: Local MCP server with Python
  # local_python_tools:
  #   protocol: mcp
  #   transport:
  #     type: stdio
  #     command: python
  #     args:
  #       - "-m"
  #       - "my_mcp_server"
  #     env:
  #       PYTHONPATH: /home/user/projects/mcp-server
  #       DEBUG: "true"

  # Example: Docker-based MCP server
  # docker_mcp_server:
  #   protocol: mcp
  #   transport:
  #     type: stdio
  #     command: docker
  #     args:
  #       - "run"
  #       - "-i"
  #       - "--rm"
  #       - "mcp/server:latest"
  #     env:
  #       API_KEY: ${MCP_API_KEY}

  # Example: OCI package (auto-pulls Docker image)
  # spotdb_mcp:
  #   protocol: mcp
  #   protocolVersion: 2024-11-05
  #   transport:
  #     package:
  #       name: docker.io/aliengiraffe/spotdb:0.1.0
  #       registryType: oci
  #     env:
  #       X_API_KEY: ${SPOTDB_API_KEY}

  # Example: OCI package with resource limits
  # heavy_mcp_server:
  #   protocol: mcp
  #   transport:
  #     package:
  #       name: ghcr.io/company/mcp-server:latest
  #       registryType: oci
  #     memory: "512m"
  #     cpus: "0.5"
  #     network: "host"
  #     env:
  #       DEBUG: "true"
